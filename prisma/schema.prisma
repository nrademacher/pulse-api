// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id      Int      @id @default(autoincrement())
  email   String   @unique
  name    String?
  profile Profile? @relation(name: "ProfileToUser")
}

model Profile {
  id          Int          @id @default(autoincrement())
  user        User         @relation(name: "ProfileToUser", fields: [userId], references: [id])
  userId      Int          @unique
  bio         String?
  collections Collection[] @relation(name: "CollectionToProfile")
}

model Collection {
  id        Int      @id @default(autoincrement())
  profile   Profile  @relation(name: "CollectionToProfile", fields: [profileId], references: [id])
  profileId Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title     String
  boards    Board[]  @relation(name: "BoardToCollection")
}

model Board {
  id           Int        @id @default(autoincrement())
  collection   Collection @relation(name: "BoardToCollection", fields: [collectionId], references: [id])
  collectionId Int
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  title        String
  lists        List[]     @relation(name: "BoardToList")
}

model List {
  id        Int      @id @default(autoincrement())
  board     Board    @relation(name: "BoardToList", fields: [boardId], references: [id])
  boardId   Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title     String
  cards     Card[]   @relation(name: "CardToList")
}

model Card {
  id        Int      @id @default(autoincrement())
  list      List     @relation(name: "CardToList", fields: [listId], references: [id])
  listId    Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title     String
  content   String?
}
